#include <bits/stdc++.h>

using namespace std;
using ll = long long;

int main() {
    ios::sync_with_stdio(false);
    string s;
    int t;
    cin >> t;
    while (t--) {
        cin >> s;
        cout << s.length() << endl;
    }
}
